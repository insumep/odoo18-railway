
                // Simplified color-contrast function to replicate bootstrap in report preview.
                @function preview-color-contrast($background) {
                    @if (lightness($background) > 50) {
                        @return black;
                    } @else {
                        @return white;
                    }
                }

                .o_company_1_layout {
                    font-family: Lato;

                    h2 {
                        color: #241f31;
                    }

                    #informations strong {
                        color: #241f31;
                    }

                    .o_total strong {
                        color: #241f31;
                    }

                    .o_company_tagline {
                        color: #241f31
                    }
                & .o_folder_header_container {
                    rect, path {
                        fill: mix(white, #241f31, 92%);
                    }
                }

                &.o_report_layout_folder_footer {
                    border-top: 1px solid #241f31;
                }
            }